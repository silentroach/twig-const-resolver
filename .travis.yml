language: php

php:
  - 5.6

cache:
  directories:
    - vendor

install:
  - composer install

script:
  - mkdir -p build/logs
  - phpunit --coverage-clover clover.xml

after_script:
  - vendor/bin/coveralls
